import React, { PureComponent } from "react";
// import ReactExport from "react-export-excel";

// const ExcelFile = ReactExport.ExcelFile;
// const ExcelSheet = ReactExport.ExcelFile.ExcelSheet;
// const ExcelColumn = ReactExport.ExcelFile.ExcelColumn;

class ExcelExport extends PureComponent {



    render() {
        // const ExcelColumnDef=this.props.excelColumnDef.map((item,index) => (
        //     <ExcelColumn key={index} label={item.title} value={item.dataIndex}/>
        //   ));
        return (
            // <ExcelFile filename={this.props.title} element={ <span > دریافت فایل اکسل</span>}>
            //     <ExcelSheet data={this.props.rowData} name={this.props.title}>
            //         {ExcelColumnDef}
            //     </ExcelSheet>
            // </ExcelFile>
            <div></div>
        );
    }
}

export default ExcelExport